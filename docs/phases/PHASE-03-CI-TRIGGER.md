# Phase 03 CI Trigger

This marker exists only to trigger the blocking CI workflow on `main` after the Phase 03 capability-passport implementation.

The Phase 03 gate remains authoritative in `.github/workflows/ci.yml`.
